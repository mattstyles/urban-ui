import { Button } from "./button.js";

/**
 * Placeholder scene — one export = one screenshot. Real state/prop coverage
 * lands in Phase 3 when the workbench globs scenes onto routes and VRT runs.
 */
export function Base() {
  return <Button>Button</Button>;
}
