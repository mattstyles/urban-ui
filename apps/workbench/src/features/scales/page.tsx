import { Page } from "../../ui/page.js";

export function ScalesPage() {
  return (
    <Page
      title="Scales"
      description="Theme scale groups — colour, space, radii. Nothing here yet: the scale groups land with the real token architecture."
    />
  );
}
