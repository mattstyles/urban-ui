import { Page } from "../../ui/page.js";

export function PlaygroundPage() {
  return (
    <Page
      title="Playground"
      description="Compositions built with the system as shipped — pages that reach only for published tokens and components. Nothing here yet."
    />
  );
}
