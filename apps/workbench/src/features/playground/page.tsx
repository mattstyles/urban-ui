import { Page } from "../../ui/page.js";

export function PlaygroundPage() {
  return (
    <Page
      title="Playground"
      description="Ad-hoc experiments and compositions, one page per experiment. Nothing here yet."
    />
  );
}
