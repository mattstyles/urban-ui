import { Page } from "../../ui/page.js";

export function LabsPage() {
  return (
    <Page
      title="Labs"
      description="Structured experiments with knobs for tweaking values live — gradients, panel backgrounds, and the like. Nothing here yet."
    />
  );
}
