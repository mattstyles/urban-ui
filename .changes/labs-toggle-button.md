---
labs: minor
---

`@urban-ui/labs` opens the experimental channel with `ToggleButton` — an
accessible two-state control wrapping react-aria-components, styled through
theme tokens, selection exposed as `aria-pressed`. Labs APIs move fast and
graduate to `@urban-ui/react` when proven.
