---
labs: patch
---

`ToggleButton` restyles onto the theme contract: colours come from the
`neutral`/`accent` scales (`fill`×`onFill` when selected), spacing from the
shared size ramp, and the probe radius gives way to the square baseline.
API unchanged.
