{
  "name": "@urban-ui/extractor",
  "version": "0.0.0",
  "private": true,
  "description": "Internal manifest extractor and prose validator — generates urban-manifest.json and enforces the package-anatomy contract. Never published.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint src test"
  },
  "dependencies": {
    "ts-morph": "28.0.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@urban-ui/config": "workspace:*"
  }
}
