{
  "name": "@urban-ui/exfil",
  "version": "0.0.0",
  "private": true,
  "description": "Internal release mechanics — intent parsing, version math, CI gates, and release assembly. Never published.",
  "bin": {
    "exfil": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint src test"
  },
  "dependencies": {
    "@urban-ui/workspace": "workspace:*",
    "citty": "0.2.2"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@urban-ui/config": "workspace:*"
  }
}
